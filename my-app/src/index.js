import ReactDOM from 'react-dom'
import { App } from './App'

const root = document.querySelector('#root')

ReactDOM.render(<App />, root)

// I started the application using npm start command from powershell. It opens a new tab on the browser running the app.